def search():
    '''
    mark connected ones in visited
    '''

class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        #visited = [n,m] init: all 0es
        '''
        
        for i in range(n):
            for j in range(m):
                if visited:
                    continue
                if zero:
                    continue
                if one:
                    tmp = search()
                if best < tmp:
                    best = tmp
        return best
        '''
        